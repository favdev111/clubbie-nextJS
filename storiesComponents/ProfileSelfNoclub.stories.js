import React from "react";

import ProfilePageNoclub from "../pages/profile-self-noclub";

export default {
  title: "Pages/Profile Self No Club",
  component: ProfilePageNoclub,
};

const Template = (args) => <ProfilePageNoclub {...args} />;

export const ProfileSelfNoClub = Template.bind({});
